# Documentación: Sistema Tickets

Esta carpeta contiene la landing de documentación del proyecto. Edita `index.html` y `style.css` para personalizar contenido y estilos.

Ver localmente (Windows + XAMPP):

1. Copia la carpeta `docs` dentro de `C:\xampp81\htdocs\sistema-tickets`:

```
xcopy /E /I "C:\Users\domin\Documenacion-tickets\docs" "C:\xampp81\htdocs\sistema-tickets\docs"
```

2. Abre en el navegador: `http://localhost/sistema-tickets/docs/`

Siguientes pasos sugeridos:
- Añadir capturas en `docs/assets/`.
- Rellenar ejemplos reales de API y SQL de migración.
- Integrar esta carpeta dentro del repositorio principal.
